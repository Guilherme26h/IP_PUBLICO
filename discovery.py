import socket as s
from dotenv import load_dotenv
config = load_dotenv(".env")

#Variaveis Globais

 host = 

def enviarEmail():
    

def pegarIp()
    ip = s.gethostbyname(host)
    enviarEmail(host)